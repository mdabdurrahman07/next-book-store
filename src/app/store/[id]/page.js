import React from 'react';

const BookDetails = ({params}) => {
    const id = params
    console.log(id)
    return (
        <div>
            BookDetails
        </div>
    );
};

export default BookDetails;